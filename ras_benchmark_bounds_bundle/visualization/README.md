# Visualization

`plot_space_time.py` consumes the standard `schedule.csv` generated by the
pipeline and writes a PNG space-time diagram. It plots each train trajectory,
uses dashed segments for siding movements, and displays validator status and
conflict count in the title.
